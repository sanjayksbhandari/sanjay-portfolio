# Certificates library

Drop certificate images (`.png`, `.jpg`, `.jpeg`, `.webp`) or PDFs (`.pdf`)
into this folder. The Education page gallery reads them automatically at
build/request time — filenames become display titles (dashes/underscores
become spaces). Do not invent certificate names in code.
